<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <TextView
        android:id="@+id/wakeEndTimeTxt"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:text="@string/txt_set_wake_end_in_time" />

    <Spinner
        android:id="@+id/wakeEndSpinner"
        android:layout_width="100dp"
        android:layout_height="wrap_content"
        android:layout_below="@+id/wakeEndTimeTxt"
        android:layout_marginTop="20dp" />

    <EditText
        android:id="@+id/wakeEndTime"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/wakeEndTimeTxt"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="18dp"
        android:ems="4"
        android:inputType="time"
        android:maxLength="8" />

    <TextView
        android:id="@+id/wakeEndStatusTxt"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginLeft="20dp"
        android:layout_marginStart="20dp"
        android:layout_alignBottom="@+id/wakeEndTime"
        android:layout_toRightOf="@+id/wakeEndTime"
        android:layout_toEndOf="@+id/wakeEndTime"
        android:text="@string/txt_status" />

</RelativeLayout>